---
title: API Reference
description: The core HTTP endpoints exposed by the Mesh for MCP, agents, OAuth, and ops
icon: Code
---

import Callout from "../../../components/ui/Callout.astro";

<Callout type="info">
  This page is a work in progress. The API reference is being expanded with detailed documentation for all endpoints and built-in tools.
</Callout>

## MCP endpoints

### Management MCP

- `POST /mcp`

This endpoint exposes **management tools** via MCP (orgs, connections, agents, API keys, monitoring, event bus).

### Connection proxy

- `POST /mcp/:connectionId`

Proxies MCP requests to a single upstream connection.

### Agent (virtual server)

- `POST /mcp/agent/:agentId`
- `POST /mcp/agent` (default Agent; requires `x-org-id` or `x-org-slug`)

Aggregates tools/resources/prompts across multiple connections.

## OAuth discovery

- `GET /.well-known/oauth-protected-resource`
- `GET /.well-known/oauth-authorization-server`

## Ops

- `GET /health`
- `GET /metrics`

## Built-in Tools

deco CMS provides built-in tools for orchestrating tasks and managing resources. For detailed documentation of each tool, see the [Built-in Tools Reference](/en/mcp-mesh/api-reference/built-in-tools).


