/**
 * Feature flags for the Mesh frontend.
 *
 * Flip these booleans to enable/disable features behind gates.
 */

/** When true, projects are visible in the org sidebar and account switcher. */
export const ENABLE_PROJECTS = true;
