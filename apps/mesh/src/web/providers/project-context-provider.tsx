/**
 * Project context provider re-exported from @decocms/mesh-sdk for backwards compatibility.
 * New code should import from @decocms/mesh-sdk directly.
 */

export {
  ProjectContextProvider,
  useProjectContext,
  Locator,
  type ProjectContextProviderProps,
  type ProjectLocator,
} from "@decocms/mesh-sdk";
