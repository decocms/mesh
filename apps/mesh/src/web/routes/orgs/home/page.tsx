/**
 * Organization Home Page
 *
 * Reuses AgentHomePage which resolves the main view from URL search params
 * (?main=ext-apps, ?main=automation, etc.) via VirtualMCPContext.
 */

export { default } from "@/web/routes/agent-home";
