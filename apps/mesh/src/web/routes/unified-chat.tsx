/**
 * Unified chat route content.
 *
 * The three-panel layout is owned by `agentShellLayout`. This component renders
 * into the right panel's "main" tab via `<Outlet />`. When no plugin sub-route is
 * active, the main tab falls back to rendering this — which defers to the tab
 * system (system/agent/expanded tabs are rendered by MainPanelWithTabs itself).
 */
export default function UnifiedChatPage() {
  return null;
}
