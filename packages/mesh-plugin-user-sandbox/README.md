# User Sandbox Plugin

Server plugin for MCP Mesh that enables multi-tenant user sandboxing.

## Features

- Create sandboxed Virtual MCPs for external users
- Session-based connect flow for user onboarding
- Per-user agent isolation with template-based configuration

## Usage

Enable the plugin in your Mesh configuration by adding `mesh-plugin-user-sandbox` to the enabled plugins list.
